services:
  parser:
    image: rialcom-parser:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PYPI_INDEX_URL: ${PYPI_INDEX_URL:-https://pypi.org/simple}
    restart: "no"
    volumes:
      - ./output:/app/output
